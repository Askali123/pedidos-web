@inherits LayoutComponentBase
@layout CatalogoPedidos.Web.Components.Layout.MainLayout

@* El título/breadcrumb de cada sección lo fija la página individual vía
   PageHeaderState — acá solo va la navegación lateral entre secciones, sin
   repetir un título propio (mismo criterio que se le va a aplicar al resto de
   la app en docs/PLAN_MEJORAS_UI_UX.md #6). *@
<div class="grid grid-cols-1 lg:grid-cols-4 gap-6 items-start">
    <div class="lg:col-span-1">
        <ManageNavMenu />
    </div>
    <div class="lg:col-span-3">
        @Body
    </div>
</div>
