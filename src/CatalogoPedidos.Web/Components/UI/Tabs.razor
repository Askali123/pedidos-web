@* Cabecera de pestañas. El contenido de cada pestaña lo decide el padre según ActiveIndex. *@
<div class="border-b border-border-subtle">
    <nav class="flex gap-6 -mb-px">
        @for (var i = 0; i < Labels.Count; i++)
        {
            var index = i;
            var active = index == ActiveIndex;
            <button type="button"
                    class="@("py-3 text-sm font-medium border-b-2 transition-colors " + (active ? "border-primary-400 text-heading" : "border-transparent text-muted hover:text-body hover:border-border-strong"))"
                    @onclick="() => Seleccionar(index)">
                @Labels[index]
            </button>
        }
    </nav>
</div>

@code {
    [Parameter] public List<string> Labels { get; set; } = new();
    [Parameter] public int ActiveIndex { get; set; }
    [Parameter] public EventCallback<int> ActiveIndexChanged { get; set; }

    private async Task Seleccionar(int index)
    {
        ActiveIndex = index;
        await ActiveIndexChanged.InvokeAsync(index);
    }
}
